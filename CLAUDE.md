# Bell Ringer

See **[AGENTS.md](AGENTS.md)** — the full playbook for setting this repository up for a new person, including the interview to run first, what is configurable and where, the order of operations, real costs, and the child-privacy obligations.

That file is the instruction set for Claude Code, Codex, and any other coding agent working in this repo. Read it before editing anything.

**This repository is not deployed anywhere.** It is the open-source copy of the machine; the running show lives in the private `eightclip/Bell-Ringer` and is deployed from there by `cd site && vercel deploy --prod --yes`. A change made here to fix something on a live site fixes nothing — make it there first, then port it back.
